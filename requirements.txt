# Основные зависимости
streamlit==1.32.0
torch==2.1.0
torchvision==0.16.0
numpy>=1.21.0
opencv-python==4.9.0.80
Pillow==10.2.0
requests==2.31.0

# Модель для inpainting (LaMa)
lama-cleaner==0.0.14

# Дополнительно (для стабильной работы)
tqdm==4.66.0
scikit-image==0.22.0
matplotlib==3.8.0  # опционально, для отладки

# Типы для автодополнения (опционально)
typing-extensions>=4.5.0
